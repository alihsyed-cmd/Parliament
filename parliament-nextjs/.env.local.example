# Point the frontend at your API. Falls back to staging if unset.
# For local dev against staging:
NEXT_PUBLIC_PARLIAMENT_API=https://parliament-api-staging.onrender.com

# For production, set this in Vercel's Environment Variables to your prod API URL.
