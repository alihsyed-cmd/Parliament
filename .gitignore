# Environment / credentials
.env
.env.local
cookies.txt

# macOS
.DS_Store
Thumbs.db

# Python
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
.venv/
venv/
.pytest_cache/

# IDE
.vscode/
.idea/

# Frontend
frontend/.next/
frontend/node_modules/
frontend/tsconfig.tsbuildinfo
.vercel/

# Pipeline staging — per-run working files, disposable once the run's
# canonical output is written to data/<slug>/
data/_staging/

# Pre-pipeline data, moved out of the canonical tree for reference only
_archive_pre_pipeline/

# Source photos — kept locally; served copy lives in frontend/public/mp-photos/
data/federal/mp_photos/

# Local design/reference material at the repo root, not build inputs.
# Root-anchored so tracked boundary-source archives under data/ are unaffected.
/*.zip
/screenshots/
/parliamentapp.ca.txt
/Parliament_Roadmap_v3.docx
